# CI-CD.md — v1

## CI goals
- Lint + typecheck frontend
- Lint backend (ruff) + run unit tests
- Build docker images (optional)

## Deployment (v1 demo)
- Docker Compose on a single server
- Environment via `.env`
- Storage mounted volumes

Production hardening is out of v1 scope.

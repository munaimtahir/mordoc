# TESTS.md — v1

## Backend
- Unit tests for:
  - section tree ordering
  - merge behavior
  - status transitions + lock/reopen
  - block schema validation
  - export job creation

## Frontend
- Basic component tests (optional)
- End-to-end checklist manual in v1 is acceptable
